# --- Core (W1-03) ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.32
pydantic[email]>=2.7,<3
pydantic-settings>=2.3,<3

# --- DB (W1-03) ---
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.29,<0.31
alembic>=1.13,<2

# --- Auth / Crypto (W1-04 / W1-06) ---
python-jose[cryptography]>=3.3,<4
argon2-cffi>=23.1,<24
cryptography>=42,<44

# --- Logging / HTTP (W1-04) ---
structlog>=24,<25
httpx>=0.27,<0.29

# --- Templates (W2-09) ---
jinja2>=3.1,<4

# --- Scheduler (W3-01) ---
apscheduler>=3.10,<4

# --- AI SDK (W1-12 / W2-08) ---
google-genai>=0.5
anthropic>=0.34,<1
openai>=1.40,<2

# --- Google APIs (W1-10) ---
google-auth>=2.30,<3
google-auth-oauthlib>=1.2,<2
google-api-python-client>=2.130,<3
google-analytics-data>=0.18,<1

# --- Schema audit / RSS / Mail (W2-04 / W2-06 / W3-02) ---
beautifulsoup4>=4.12,<5
lxml>=5
feedparser>=6.0,<7
resend>=2.0,<3
weasyprint>=66,<70  # 月次レポート PDF 出力 (F. 共有 URL)

# --- Headless browser(AI Overviews 自前取得用)---
playwright>=1.50,<2

# --- Dev ---
ruff>=0.6
mypy>=1.10
pytest>=8.0
pytest-asyncio>=0.24
aiosqlite>=0.20  # ユニットテストでの in-memory DB 用
